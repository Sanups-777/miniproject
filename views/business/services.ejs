<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      @import url("https://fonts.googleapis.com/css?family=Poppins:400,500,600,700&display=swap");
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Poppins", sans-serif;
      }
      html,
      body {
        height: 100%;
        width: 100%;
        display: flex;
        justify-content: center; /* Aligning horizontally */
        align-items: center; /* Aligning vertically */
        background: -webkit-linear-gradient(
          left,
          rgba(33, 8, 28, 1),
          #4d1434,
          rgba(77, 20, 52, 1)
        );
      }
      .wrapper {
        position: relative;
        display: flex;
        flex-direction: column; /* Aligning items in a column */
        align-items: center; /* Aligning items to the center horizontally */
        width: 40%; /* Adjusting width to fill the screen */
        background: #fff;
        padding: 30px;
        border-radius: 15px;
        box-shadow: 0px 15px 20px rgba(0, 0, 0, 0.1);
        margin-top: 20px; /* Adding margin to separate the wrappers */
      }
      .containerb {
        display: flex;
        flex-direction: column; /* Align items in a column */
        align-items: center; /* Align items to the center horizontally */
        width: 80%;
      }
      .form-inner form .field {
        height: 50px;
        width: 100%;
        margin-top: 20px;
        z-index: 2;
      }
      .form-inner form .field input {
        height: 50px; /* Adjust height */
        width: 100%;
        outline: none;
        padding-left: 15px;
        border-radius: 15px;
        border: 1px solid lightgrey;
        border-bottom-width: 2px;
        font-size: 17px;
        transition: all 0.3s ease;
        resize: none; /* Disable resizing */
        padding-right: 15px;
      }
      .form-inner form .field input:focus::placeholder {
        color: #4d1434;
      }
      .form-inner form .field input:hover {
        background-color: transparent;
        color: inherit;
      }
      .form-inner form .field input:hover {
        outline: none;
      }
      .form-wrapper {
        display: none;
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background: #fff;
        padding: 30px;
        border-radius: 15px;
        box-shadow: 0px 15px 20px rgba(0, 0, 0, 0.1);
        z-index: 2;
        width: 450px;
        height: 350px;
      }
      .show-form-button {
        background: #4d1434; /* Adjusting button color */
        color: white;
        border: none;
        border-radius: 10px;
        padding: 10px 20px;
        font-size: 16px;
        cursor: pointer;
        margin-top: 20px;
        z-index: 2;
      }
    </style>
  </head>
  <%- include('../nav-footer/navbardisplay') %>
  <body>
    <div class="containerb">
      <div class="wrapper">
        <h3>Add services</h3>
        <div class="form-inner">
          <form action="/homesaver/Business/acceptservice" method="post">
            <div class="field">
              <input type="hidden" id="email" name="id" value="<%= bId %>" />
            </div>
            <div class="field">
              <input
                type="text"
                id="service"
                name="service"
                placeholder="Enter service provided"
                required
              />
            </div>
            <div class="field">
              <input
                type="number"
                id="minprice"
                name="minprice"
                placeholder="Enter minprice"
                required
              />
            </div>
            <div class="field">
              <input
                type="number"
                id="maxprice"
                name="maxprice"
                placeholder="Enter maxprice"
                required
              />
            </div>
            <% if (!business.description) { %>
            <div class="field">
              <input
                type="text"
                id="description"
                name="description"
                placeholder="Enter Description"
                required
              />
            </div>
            <% } else { %>
            <div class="field">
              <input
                type="hidden"
                id="description"
                name="description"
                value="null"
              />
            </div>
            <% } %>
            <div class="field btn">
              <input class="show-form-button" type="submit" value="Enter" />
            </div>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
