<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        background-color: #f4f4f4; /* Set a light background color */
        font-family: Arial, sans-serif; /* Set a fallback font */
        margin: 0;
        padding: 0;
      }

      th,
      td {
        text-align: left;
        border-bottom: 1px solid #ddd;
        background-color: #fff;
        max-width: 20px;
      }

      th {
        background-color: #f2f2f2; /* Light gray background for header */
      }

      tr:nth-child(even) {
        background-color: #f9f9f9; /* Alternate row background color */
      }

      .abutton {
        padding: 8px 12px;
        border: none;
        border-radius: 4px;
        background: -webkit-linear-gradient(
        left,
        rgba(33, 8, 28, 1),
        #4d1434,
        rgba(77, 20, 52, 1)
      ); 
        color: #fff; /* White text color */
        cursor: pointer;
        text-decoration: none;
      }

      .abutton:hover {
        background-color: #4d1434;;
        color: white;
        text-decoration: none; /* Darker blue on hover */
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h3>services provided</h3>
      <table class="table">
        <form action="/homesaver/Business/service" method="post">
          <div class="field">
            <input type="hidden" id="email" name="id" value="<%= bId %>" />
          </div>
          <div class="field">
            <input type="text" id="service" name="service" placeholder="Enter service provided" required />
          </div>
          <div class="field">
            <input type="number" id="minprice" name="minprice" placeholder="Enter minprice" required />
          </div>
          <div class="field">
            <input type="number" id="maxprice" name="maxprice" placeholder="Enter maxprice" required />
          </div>
          <div class="field btn">
            <div class="btn-layer"></div>
            <input type="submit" value="Enter" />
          </div>
        </form>
        
          
        </tbody>
      </table>
    </div>
  </body>
</html>
